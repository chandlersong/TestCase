dubbo:
  application:
    name: goServer # metadata: application=myApp; name=myApp
  registries:
    nacosWithCustomGroup:
      protocol: nacos
      address: nacos-cs:8848
  protocols:
    dubbo:
      name: dubbo
      port: 20000
  provider:
    services:
      UserProvider: # 服务提供结构类名
        interface: org.example.ai.AiService # 接口需要与 go/java 客户端对应